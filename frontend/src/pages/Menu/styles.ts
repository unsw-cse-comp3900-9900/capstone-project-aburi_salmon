import { createStyles, Theme } from "@material-ui/core";

export const styles = (theme: Theme) =>
  createStyles({
    menupage: {
      height: '97vh'
    },
    title: {
      margin: '5px'
    }
  });
  