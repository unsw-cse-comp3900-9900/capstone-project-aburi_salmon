import { createStyles, Theme, fade } from "@material-ui/core";

export const styles = (theme: Theme) =>
  createStyles({
    waitingpage: {

    },
    title: {
      margin: '30px',
      fontSize: '40px',
      height: '100%'
    }
  });